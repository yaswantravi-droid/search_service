#common for all services
CACHE_HOST=localhost
CACHE_PORT=6379
CACHE_TLS_ENABLED=False

#service dependent envs
MONGODB_URL="mongodb://localhost:27017/interactly"
SEARCH_CATEGORIES="bots"

# CORS Configuration
# Use "*" to allow all origins (development only)
# For production, specify specific origins: "https://yourdomain.com,https://app.yourdomain.com"
CORS_ORIGINS="*"
